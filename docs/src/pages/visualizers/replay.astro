---
import BaseLayout from '../../layouts/BaseLayout.astro';
import LedgerScrubber from '../../components/visualizers/LedgerScrubber.astro';
---

<BaseLayout
  title="Ledger Replay Scrubber"
  description="Byte-identical deterministic replay scrubber for The Shattered Kingdoms engine ledger."
>
  <article class="visualizer-page">
    <nav aria-label="Breadcrumb" class="breadcrumbs">
      <ol>
        <li><a href="/">Home</a></li>
        <li><a href="/visualizers">Visualizers</a></li>
        <li aria-current="page">Ledger Replay Scrubber</li>
      </ol>
    </nav>

    <header class="page-header">
      <h1 class="page-title">Ledger Replay Scrubber</h1>
      <p class="lead">
        Scrub forward and backward through a serialized ledger of engine events.
        Because every input is captured in order and the simulation is
        deterministic, each sequence reproduces the same world snapshot byte for
        byte.
      </p>
    </header>

    <LedgerScrubber />
  </article>
</BaseLayout>

<style>
  .visualizer-page {
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: calc(var(--header-height) + 2rem) var(--content-padding) 3rem;
  }

  .breadcrumbs {
    margin-bottom: 1.5rem;
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: var(--gold-deep);
  }

  .breadcrumbs ol {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .breadcrumbs li:not(:last-child)::after {
    content: '›';
    margin-left: 0.5rem;
    color: var(--border-stone);
  }

  .breadcrumbs a {
    color: var(--gold-primary);
    text-decoration: none;
  }

  .breadcrumbs a:hover {
    color: var(--gold-bright);
    text-decoration: underline;
  }

  .page-header {
    margin-bottom: 2rem;
  }

  .page-title {
    margin: 0 0 0.75rem;
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--gold-primary);
    text-shadow: var(--shadow-gold-sm);
  }

  .lead {
    max-width: 64ch;
    margin: 0;
    font-family: var(--font-body);
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--parchment-muted);
  }
</style>
