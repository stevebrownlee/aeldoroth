---
'@type': CreativeWork
identifier: "51"
title: "Plan 6 acceptance harness: brain prompts audited via final-world sweep because Brain GenServers escape in-process Scripted capture"
created: 2026-08-19T22:55:21Z
subject:
  - shards-engine
  - acceptance
  - truth-barrier
  - plan-6
description: Scripted.take_requests drains the process dictionary, but deliberate/adopt calls execute inside Brain GenServer processes, so 13 of 17 prompts escaped the capture audit. The prompt builders (Agents.Prompt) are pure functions of the slice, and allowed information sets grow monotonically within a run (beliefs/envelopes only accumulate), so sweeping every tier-3 agent's prompt over the FINAL world flags any leak class that could have fired earlier — sound over-approximation without touching the pipeline. Interpret prompts are built pre-phase (before declare mutates world), so captures pair [pre, post] worlds and a name is a leak only if non-local in both brackets.
---
# Plan 6 acceptance harness: brain prompts audited via final-world sweep because Brain GenServers escape in-process Scripted capture

```json
{
  "access_count": 0,
  "archived": 0,
  "commit_sha": "498fe983d20234079ef188d02a60a4cf1d89a5d0",
  "id": 51,
  "implementation_details": null,
  "importance": 7,
  "last_accessed_at": null,
  "rationale": "Scripted.take_requests drains the process dictionary, but deliberate/adopt calls execute inside Brain GenServer processes, so 13 of 17 prompts escaped the capture audit. The prompt builders (Agents.Prompt) are pure functions of the slice, and allowed information sets grow monotonically within a run (beliefs/envelopes only accumulate), so sweeping every tier-3 agent's prompt over the FINAL world flags any leak class that could have fired earlier — sound over-approximation without touching the pipeline. Interpret prompts are built pre-phase (before declare mutates world), so captures pair [pre, post] worlds and a name is a leak only if non-local in both brackets.",
  "status": "active",
  "summary": "Plan 6 acceptance harness: brain prompts audited via final-world sweep because Brain GenServers escape in-process Scripted capture",
  "tags": [
    "shards-engine",
    "acceptance",
    "truth-barrier",
    "plan-6"
  ],
  "timestamp": "2026-08-19T22:55:21Z",
  "uuid": "1ac9b237-5cb1-4c76-be20-184bec2de478"
}
```
