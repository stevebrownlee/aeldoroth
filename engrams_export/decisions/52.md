---
'@type': CreativeWork
identifier: "52"
title: "client_web LiveView client: player seats are wire-protocol clients, host console is trusted"
created: 2026-08-20T02:04:22Z
subject:
  - client-web
  - liveview
  - wire-protocol
  - trust-split
  - plan-7
description: "Plan 7 built the web client (Phoenix LiveView 1.2, vendored assets, no esbuild). Split trust: HomeLive + SpectateLive are referee-console surfaces calling Session directly (run creation, roster, advance lever); RunLive is a player seat talking ONLY the wire protocol through a real ClientTUI.Conn on loopback — truth barrier by construction. One endpoint serves both /live (LiveView socket, browser pipeline with protect_from_forgery + session cookie) and /socket (Wire.Socket verbatim). Zero-routing default plays via grammar/template fallback (verified). browser pipeline needs fetch_session + protect_from_forgery before put_root_layout or every LiveView join dies 'stale'; Plug.Static tuple from joins app_dir directly so assets need 'priv/static/assets'. Wire fixes en route: JSONSafe boundary projects ledger events to JSON-safe terms (hex md5 hash, structs→maps) for spectate tails; resume reply carries resumed:true to stay distinct from heartbeat acks; ooc push now implemented (was documented but missing)."
---
# client_web LiveView client: player seats are wire-protocol clients, host console is trusted

```json
{
  "access_count": 0,
  "archived": 0,
  "commit_sha": "4ebbe5b0baa9800b616cc962866deda3ebc6cca6",
  "contract": null,
  "id": 52,
  "implementation_details": null,
  "importance": 8,
  "last_accessed_at": null,
  "rationale": "Plan 7 built the web client (Phoenix LiveView 1.2, vendored assets, no esbuild). Split trust: HomeLive + SpectateLive are referee-console surfaces calling Session directly (run creation, roster, advance lever); RunLive is a player seat talking ONLY the wire protocol through a real ClientTUI.Conn on loopback — truth barrier by construction. One endpoint serves both /live (LiveView socket, browser pipeline with protect_from_forgery + session cookie) and /socket (Wire.Socket verbatim). Zero-routing default plays via grammar/template fallback (verified). browser pipeline needs fetch_session + protect_from_forgery before put_root_layout or every LiveView join dies 'stale'; Plug.Static tuple from joins app_dir directly so assets need 'priv/static/assets'. Wire fixes en route: JSONSafe boundary projects ledger events to JSON-safe terms (hex md5 hash, structs→maps) for spectate tails; resume reply carries resumed:true to stay distinct from heartbeat acks; ooc push now implemented (was documented but missing).",
  "status": "active",
  "summary": "client_web LiveView client: player seats are wire-protocol clients, host console is trusted",
  "tags": [
    "client-web",
    "liveview",
    "wire-protocol",
    "trust-split",
    "plan-7"
  ],
  "timestamp": "2026-08-20T02:04:22Z",
  "uuid": "159e0202-1da4-431d-90ac-154de572fd89"
}
```
